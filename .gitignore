# Settings with secrets (use settings.example.json as template)
server/config/settings.json
server/config/mcp_servers.json

# Database
server/*.db
server/*.db-wal
server/*.db-shm
server/database/*.db
server/database/*.db-wal
server/database/*.db-shm

# App MCP config (per-user runtime)
server/config/app_mcp.json

# Grok auth cookies (session cookies for grok.com — never commit)
server/config/grok_download_history.json
tools/grok/grok_download_history.json
tools/grok_download_history.json

# Tripo3D credentials + runtime captures — never commit
tools/tripo/tripo_config.json
tools/tripo/tripo_studio_token.json
tools/tripo/tripo_capture.json
tools/tripo/tripo_generate_capture.json

# Animation Generator per-user generated content
tools/animation_generator/projects/

# Windows shortcuts
*.lnk

# Logs
server/api/server_crash.log
server/logs/
app/*.log
app/flutter_*.log
*.log

# Temp / cache
server/tmp/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Flutter
app/.dart_tool/
app/.packages
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/.kotlin/
app/assets/generated/
app/asset_descriptor.json

# Windows build output in root (from flutter build)
*.exe
*.dll
*.pdb
data/

# Op-log sync runtime state (generated by server at runtime — not versioned)
server/data/
server/data/event_log.jsonl
server/data/event_log.*.jsonl
server/data/client_cursors.json

# Signing keys (auto-generated or user-provided)
keys/

# Per-app runtime data
tasklist.json
tasklist.json.bak
task_attachments/
task_archives/
auto_build_logs/

# Automation scripts (auto-generated per app)
server/automations/*.sh

# Worker config with account details
worker/wrangler.toml

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.code-workspace
server/automations/configs.json

# Character Creator (Godot project, WIP)
tools/character_creator/

# CCGS personal-use bundle (director gates + 49 agents + 72 skills + hooks + rules
# + skill testing framework). Copied verbatim from github.com/Donchitos/Claude-Code-Game-Studios
# for interactive personal dev sessions. Not part of AGB's autonomous pipeline — never commit.
tools/ccgs/

# Pixel Guy (merged into tools/) — user-local state and build artifacts
tools/pixel_guy/projects.json
tools/pixel_guy/state.json
tools/pixel_guy/tasklist.json
tools/pixel_guy/build/
tools/pixel_guy/assets/characters/
tools/pixel_guy/**/.dart_tool/
tools/pixel_guy/**/.flutter-plugins
tools/pixel_guy/**/.flutter-plugins-dependencies
# Flutter Windows generated files (CMakeLists.txt is the only committed file)
tools/pixel_guy/windows/flutter/ephemeral/
tools/pixel_guy/windows/flutter/generated_plugin_registrant.cc
tools/pixel_guy/windows/flutter/generated_plugin_registrant.h
tools/pixel_guy/windows/flutter/generated_plugins.cmake

# DB backups from maintenance ops
server/*.db.bak_*

# Comic Translator (merged into tools/) — runtime local overrides
tools/comic_translator/config.local.json
