pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

// The AAR file names come from this: LcInAppUpdate-debug.aar / LcInAppUpdate-release.aar
rootProject.name = 'LcInAppUpdate'
